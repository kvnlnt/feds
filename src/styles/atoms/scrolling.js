const params = require("../params.js");
module.exports = {
    scroll: {
        overflow: {
            auto: {
                overflow: "auto"
            },
            y: {
                overflowY: "auto"
            }
        }

    }
};